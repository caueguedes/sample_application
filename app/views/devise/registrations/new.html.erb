<%= react_component('admin/pages/Register', {
  links: {
    log_in: new_session_path(resource_name),
    register: registration_path(resource_name)
  },
  csrf_token: form_authenticity_token,
  email_taken: resource.errors[:email].first == "has already been taken" || nil
}) %>
