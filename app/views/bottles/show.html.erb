<%= form_with(model: @bottle) do %>
  <%= react_component('admin/components/Forms/BottleForm', {
    title: translate("#{controller_name}.#{action_name}.title"),
    isEditing: is_editing,
    bottle: @bottle,
    errors: @bottle.errors.to_hash,
    bottlesPath: bottles_path
  }) %>
<% end %>
