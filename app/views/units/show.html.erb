<%= form_with(model: @unit) do %>
  <%= react_component('admin/components/Forms/UnitForm', {
    title: translate("#{controller_name}.#{action_name}.title"),
    isEditing: is_editing,
    unit: @unit,
    errors: @unit.errors.to_hash,
    unitsPath: units_path
  }) %>
<% end %>
